import { EliminatedNumbersPipe } from './eliminated-numbers.pipe';

describe('EliminatedNumbersPipe', () => {
  it('create an instance', () => {
    const pipe = new EliminatedNumbersPipe();
    expect(pipe).toBeTruthy();
  });
});

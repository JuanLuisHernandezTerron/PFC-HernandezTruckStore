export interface tractora{
    _id: String,
    cv: Number,
    adblue: Boolean,
    numeroDepositos: Number,
    kms: Number,
    combustible: String,
    retarder: Boolean,
    vehiculo: Array<any>
}